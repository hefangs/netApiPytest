pipeline {
    agent any

    stages {
        stage('stage 1') {
            steps {
                sh 'pwd'
                sh 'ls -al'
                sh 'll'
            }
        }          
    }
}
